<%= simple_form_for @request, :html => { :class => 'form-horizontal' } do |f| %>

<%= f.error_notification %>
<%= f.input :content %>

<div class="form-actions">
  <%= f.button :submit %>
</div>
<% end %>