
<%= simple_form_for @venue, :url => venue_password_reset_path(params[:id]), :html => { :class => 'form-horizontal' } do |f| %>

<%= f.input :email %>
<%= f.input :password %>
<%= f.input :password_confirmation %>
<%= f.button :submit "Update Password" %>

<% end %>